{
  "compilerOptions": {
    "target": "es6", // Chọn phiên bản JavaScript mà TypeScript sẽ chuyển đổi mã nguồn
    "module": "esnext", // Cho phép sử dụng các tính năng module của ES6
    "jsx": "react", // Hỗ trợ JSX với React
    "strict": true, // Bật chế độ kiểm tra kiểu nghiêm ngặt
    "esModuleInterop": true, // Hỗ trợ import/export giữa các phiên bản module JavaScript khác nhau
    "skipLibCheck": true, // Bỏ qua kiểm tra các file .d.ts
    "forceConsistentCasingInFileNames": true, // Bật kiểm tra việc đặt tên file nhất quán
    "moduleResolution": "node", // Giải quyết module theo cách sử dụng Node.js
    "resolveJsonModule": true, // Hỗ trợ import JSON như một module
    "isolatedModules": true, // Bật chế độ xử lý độc lập cho mỗi file TypeScript
    "noEmit": true // Không tạo ra các file đầu ra
  },
  "include": [
    "src" // Chỉ định thư mục chứa mã nguồn TypeScript
  ],
  "exclude": [
    "node_modules" // Loại trừ các thư mục không nên được định nghĩa bởi TypeScript
  ]
}